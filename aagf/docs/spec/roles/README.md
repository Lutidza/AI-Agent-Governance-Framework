# roles

Здесь находятся role-спецификации агентов.

- Каждая роль MUST иметь границы ответственности и правила передачи handoff.
- Role-спецификации MUST храниться модульно через `aagf/docs/spec/roles/rules.index.yaml` + `aagf/docs/spec/roles/rules/*.yaml`.
