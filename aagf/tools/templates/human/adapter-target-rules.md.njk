<!-- GENERATED FILE: DO NOT EDIT MANUALLY -->
<!-- Source: docs/spec/manifests/docs.manifest.yaml + {{ target.runtime_rules }} -->

# rules (human projection)

Этот документ является человеко-читаемой проекцией индекса target-runtime rules для `{{ target.id }}`.

## Source mapping

- Product source: `docs/spec/**`
- Adapter runtime index source: `{{ target.runtime_rules }}`
- Runtime compatibility: `{{ target.runtime_dir }}/rules/**`
- Human projection path: `runtime/{{ target.human_runtime_dir }}/rules/**`

## Обязательные условия

- Правила MUST извлекаться из `docs/spec/**`.
- Правила MUST NOT вводить новые нормативные пункты, отсутствующие в source-слое.
- IF runtime rules расходятся с source-слоем THEN приоритет MUST быть у `docs/spec/**`.

## Модульная структура

- Runtime rules MUST быть разбиты на отдельные rule-entry файлы.
- Human projection MUST повторять модульную структуру runtime-слоя.
- IF отсутствует entry-файл для source-правила THEN это MUST считаться drift.

## Карта rule-entry файлов по секциям
{% for section in sections %}
### {{ section.title }} (`{{ section.id }}`)
{% for rule_entry in rule_entries %}
{% if rule_entry.section_id == section.id %}
- [`{{ rule_entry.entry.id }}` {{ rule_entry.entry.title }}](./{{ rule_entry.section_id }}/{{ rule_entry.entry.id }}.md)
{% endif %}
{% endfor %}
{% endfor %}
